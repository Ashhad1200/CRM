apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "softcrm.fullname" . }}-config
  labels:
    {{- include "softcrm.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
